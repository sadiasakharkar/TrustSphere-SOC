[project]
name = "trustsphere-soc"
version = "0.1.0"
description = "Offline-first SOC pipeline foundation for TrustSphere"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
